# Available Computing —— 产品设计文档

> 版本：v0.1 草案
> 日期：2026-05-05
> 状态：设计评审中
> 关联文档：[01-PRD.md](./01-PRD.md)

---

## 1. 设计原则

### 1.1 设计理念

> **「算力是一种状态，不是一种配置。」**

传统的网关产品（One API / LiteLLM）让用户「配置」哪些模型可用——这是一种静态思维。Available Computing 把模型可用性当作**实时状态**：自动发现、自动监控、自动呈现。用户的角色从「配置者」变成「观察者 + 使用者」。

### 1.2 核心设计原则

| 原则 | 含义 |
|------|------|
| **以"免费可用"为第一视角** | 所有 UI 默认只展示免费且当前可用的模型，付费模型隐藏在筛选项中 |
| **自动发现优先于手动配置** | 用户只配 Key，模型由系统自动找出来；用户**不应该**录入模型名 |
| **状态即展示** | 模型当前是否可用，看一眼就知道，无需点进去查 |
| **透明可信** | 每个"免费"判定结果都能追溯到来源（白名单 / API 字段 / 探测） |
| **可拆卸** | 用户随时可以脱离本产品——直接复制 Key + Endpoint + 模型名即可在原生 SDK 使用 |

### 1.3 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 部署形态 | 单机自托管，单容器 | 用户的 Key 不出本地，符合"不替你保管 Key"原则 |
| 用户体系 | MVP 单用户，本地密码登录 | 个人工具，复杂用户体系反而是负担 |
| Key 存储 | 本地数据库 + 加密 | 安全底线 |
| 模型来源 | API 拉取 + 白名单融合 | 单一来源不可靠，必须多源交叉 |
| 默认视图 | "可用免费模型"列表，而非"厂商列表" | 强化产品定位 |

---

## 2. 核心概念定义

设计与文档中频繁使用的术语，统一在此定义。

| 概念 | 定义 |
|------|------|
| **Provider（厂商）** | 一家提供 AI 模型 API 的公司，如 Groq、SiliconFlow、Google |
| **Channel（渠道）** | 用户配置的一条接入：一个厂商 + 一份 Key + 可选 Base URL。一个厂商可以有多个 Channel（多 Key） |
| **Model（模型）** | 一个具体可调用的模型，归属于某个 Channel |
| **Free Model（免费模型）** | 经判定当前可零成本调用的模型；判定依据见 §6 |
| **Health（健康状态）** | 模型当前的可达性与响应质量，三档：🟢 Healthy / 🟡 Slow / 🔴 Down |
| **Pool（算力池）** | 所有 Channel 下、所有"免费 + 健康"模型的并集，是产品的核心呈现对象 |
| **Adapter（适配器）** | 对接单个厂商的代码模块，封装拉取模型列表、判定免费、健康探测的逻辑 |

**实体关系：**
```
Provider (厂商定义，代码内置)
   │ 1
   │ N
Channel (用户配置的接入实例)
   │ 1
   │ N
Model (该 Channel 下发现的模型)
   │
   └── 每个 Model 携带：is_free, health_status, metadata
```

---

## 3. 信息架构

### 3.1 顶层导航

```
┌─────────────────────────────────────────┐
│  Available Computing                    │
├─────────────────────────────────────────┤
│  📊 算力池      （首页 / Dashboard）     │
│  🔌 厂商管理    （Channels）            │
│  📜 调用日志    （V1.0+）               │
│  ⚙️  设置       （Settings）             │
└─────────────────────────────────────────┘
```

### 3.2 页面层级

```
算力池 (/)
  ├─ 总览看板
  ├─ 模型列表
  └─ 模型详情 (/models/:id)

厂商管理 (/channels)
  ├─ 厂商列表
  ├─ 添加厂商 (/channels/new)
  └─ 厂商详情 (/channels/:id)

调用日志 (/logs)        [V1.0+]

设置 (/settings)
  ├─ 基础设置（探测频率、阈值）
  ├─ 白名单管理
  ├─ 调用 Key 管理（V1.0+）
  └─ 关于
```

---

## 4. 关键页面设计

### 4.1 算力池总览（首页）

**布局结构：**

```
┌──────────────────────────────────────────────────────────────┐
│  📊 算力池总览                              🔄 刷新  🔧 设置   │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌───────────┐ │
│  │ 接入厂商    │ │ 免费模型    │ │ 健康可用    │ │ 今日调用   │ │
│  │   6        │ │   23       │ │   19 🟢    │ │  1,204    │ │
│  │ /6 启用    │ │            │ │  3🟡 1🔴   │ │           │ │
│  └────────────┘ └────────────┘ └────────────┘ └───────────┘ │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  筛选：[全部▼] [文本▼] [多模态▼] [代码▼]   🔍 搜索模型...     │
│        [仅显示健康]  [仅显示免费✓]                            │
│                                                              │
│  ┌────────┬───────────────────┬────────┬────────┬───────┬───┤
│  │ 厂商    │ 模型名             │ 类型    │ 上下文  │ 状态  │   │
│  ├────────┼───────────────────┼────────┼────────┼───────┼───┤
│  │ Groq    │ llama-3.3-70b    │ 文本    │ 128K   │🟢 80ms │ ⋯ │
│  │ 硅基流动 │ Qwen2.5-72B     │ 文本    │ 32K    │🟢 320ms│ ⋯ │
│  │ 谷歌    │ gemini-2.0-flash │ 多模态  │ 1M     │🟢 540ms│ ⋯ │
│  │ Groq    │ mixtral-8x7b    │ 文本    │ 32K    │🟡 2.1s │ ⋯ │
│  │ DeepSeek│ deepseek-chat   │ 文本    │ 64K    │🔴 限流  │ ⋯ │
│  └─────────┴─────────────────┴────────┴────────┴────────┴───┘
│                                                              │
│  [显示 1-23 条 / 共 23 条]                                    │
│                                                              │
│  ── 空状态（当用户尚未添加任何厂商时，列表区替换为：） ──       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │    还没有接入任何厂商                                  │   │
│  │    添加后系统自动探测免费模型，通常 1 分钟内完成         │   │
│  │  [＋ Groq]  [＋ SiliconFlow]  [＋ Gemini]           │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

**关键交互：**
- 点击模型行 → 跳转到模型详情
- 点击模型行 → 跳转到模型详情
- 行右侧 `⋯` → 复制 Endpoint / 复制调用示例 / 刷新该模型健康状态
- 顶部 4 个统计卡片均可点击跳到对应筛选结果
- "🔄 刷新" → 触发全量重新探测，按钮带 loading 状态

**默认状态：**
- 默认勾选「仅显示免费」「仅显示健康」
- 默认按响应时间升序排列（最快在前），让用户第一眼看到最好用的模型

### 4.2 模型详情页

```
┌──────────────────────────────────────────────────────────┐
│  ← 返回   llama-3.3-70b-versatile                        │
│                                                          │
│  来自 Groq · 文本模型 · 🟢 健康 · 上下文 128K              │
├──────────────────────────────────────────────────────────┤
│  📋 基本信息                                              │
│   模型 ID:   llama-3.3-70b-versatile                     │
│   Endpoint: https://api.groq.com/openai/v1               │
│   速率限制: 30 RPM / 6,000 TPM / 14,400 RPD              │
│   免费判定: ✓ 厂商整体免费 (Groq 全免费)                  │
├──────────────────────────────────────────────────────────┤
│  📈 24 小时健康状态                                        │
│   [───────●────●●──────●●●●●──────────────]              │
│    24h ago                              now               │
│   平均响应时间：156ms · 成功率：99.2%                     │
├──────────────────────────────────────────────────────────┤
│  🚀 快速调用                                              │
│   [Tab: cURL] [Python] [Node.js] [OpenAI SDK]            │
│   ┌──────────────────────────────────────────────────┐  │
│   │ curl https://api.groq.com/openai/v1/chat/...    │  │
│   │   -H "Authorization: Bearer gsk_••••••sN3K"    │  │
│   │   -d '{"model": "llama-3.3-70b-versatile",..}'  │  │
│   └──────────────────────────────────────────────────┘  │
│   📋 复制（复制时展开为完整 Key，不脱敏）                    │
└──────────────────────────────────────────────────────────┘
```

### 4.3 厂商管理页

```
┌────────────────────────────────────────────────────────────┐
│  🔌 厂商管理                              [+ 添加厂商]      │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐ │
│  │ ✓ Groq                            🟢 12 个免费模型    │ │
│  │   API Key: gsk_••••••••••sN3K   [👁] [复制]          │ │
│  │   最后探测: 2 分钟前              [刷新] [编辑] [禁用] │ │
│  └──────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ ✓ SiliconFlow                     🟢 7 个免费模型     │ │
│  │   API Key: sk_••••••••••a1B2   [👁] [复制]           │ │
│  │   最后探测: 5 分钟前              [刷新] [编辑] [禁用] │ │
│  └──────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ ✗ Gemini                          ⚠️ 鉴权失败          │ │
│  │   API Key: AIza••••••••••       [👁]                 │ │
│  │   最后探测: 1 小时前              [重新验证] [编辑]    │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

### 4.4 添加厂商（弹窗 / 步骤化）

**Step 1：选择厂商**
```
┌────────────────────────────────────────────────┐
│  添加厂商                              ✕       │
├────────────────────────────────────────────────┤
│  选择厂商：                                     │
│  ┌────────┐ ┌────────────┐ ┌────────┐         │
│  │  Groq  │ │ SiliconFlow│ │ Gemini │         │
│  │  全免费 │ │  部分免费   │ │ 免费额度│         │
│  └────────┘ └────────────┘ └────────┘         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ DeepSeek │ │Cloudflare│ │ 智谱GLM   │       │
│  └──────────┘ └──────────┘ └──────────┘       │
│  ...                                            │
│  💡 没找到？[请求支持 →]                         │
└────────────────────────────────────────────────┘
```

**Step 2：填写 Key**
```
┌────────────────────────────────────────────────┐
│  添加厂商 - Groq                       ✕       │
├────────────────────────────────────────────────┤
│  📖 在哪里获取 Key？                            │
│     Groq 控制台 → API Keys → Create Key         │
│     [打开 Groq Console ↗]                      │
│                                                 │
│  API Key *                                      │
│  ┌──────────────────────────────────────────┐  │
│  │ gsk_xxxxxxxxxxxxxxxxxxxxxxxx             │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  Base URL（可选，默认使用官方）                  │
│  ┌──────────────────────────────────────────┐  │
│  │ https://api.groq.com/openai/v1           │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  [取消]                       [验证并添加 →]    │
└────────────────────────────────────────────────┘
```

**Step 3：自动探测进度**
```
┌────────────────────────────────────────────────┐
│  正在探测 Groq...                               │
├────────────────────────────────────────────────┤
│  ✓ 验证 Key                                    │
│  ✓ 拉取模型列表（共 18 个）                     │
│  ⟳ 判定免费模型...                             │
│  ⠂ 探测可用性...                                │
│                                                 │
│  [进度条 ━━━━━━━━━━━━━━━━━━━━ 75%]             │
└────────────────────────────────────────────────┘
```

### 4.5 设置页

```
┌────────────────────────────────────────────────────────┐
│  ⚙️ 设置                                                │
├────────────────────────────────────────────────────────┤
│  探测设置                                              │
│   自动刷新频率:    [每 6 小时 ▼]                       │
│   健康探测频率:    [每 30 分钟 ▼]                      │
│   响应时间阈值:    [1000] ms （超过则标记为🟡）         │
│                                                        │
│  白名单管理                                             │
│   当前白名单版本:  v2026.05.01                         │
│   [检查更新]  [手动编辑]                                │
│                                                        │
│  访问安全                                               │
│   登录密码:        [修改密码]                          │
│   Key 加密:        ✓ 始终开启（不可关闭）               │
│                                                        │
│  数据                                                  │
│   导出配置  导入配置  清空数据                          │
│                                                        │
│  关于                                                  │
│   版本 v0.1.0 · GitHub · 反馈                          │
└────────────────────────────────────────────────────────┘
```

---

## 5. 关键流程

### 5.1 添加厂商 → 自动发现免费模型

```
用户点击「+添加厂商」
  ↓
选择厂商类型（如 Groq）
  ↓
输入 API Key
  ↓
点击「验证并添加」
  ↓
前端立即反馈：
  Step 1 仅做 Key 验证（同步，快，< 2s）：
    调用 /openai/v1/models 确认 Key 有效
    ├─ 失败 → 报错，留在表单页面，用户修正
    └─ 成功 → 弹窗关闭，厂商卡片立即出现在厂商管理列表，显示「探测中...」状态
  ↓
后台异步任务（不阻塞 UI）：
  2. 拉取完整模型列表
  3. 对每个模型，按 §6 判定是否免费
  4. 首次健康探测
  5. 写入数据库，通过 WebSocket 推送更新
  ↓
首页 / 厂商管理页实时更新：
  「探测中...」→「🟢 12 个免费模型」
  模型逐步出现在算力池列表中（无需手动刷新）
```

### 5.2 定时自动探测流程

```
后台调度器 (每 6 小时触发)
  ↓
对每个启用的 Channel：
  ├─ 重新拉取模型列表（处理新增/下线）
  ├─ 重新判定免费状态（处理收费策略变化）
  └─ 触发一次健康探测
  ↓
更新数据库
  ↓
首页 WebSocket 推送 → UI 自动刷新统计数字
```

### 5.3 健康探测流程

**策略：被动优先，主动兜底。**

```
[被动路径] 每次真实用户调用完成后：
  拿到响应结果（成功/失败/响应时间）
  ↓
  直接作为健康信号写入历史表（零额外配额消耗）
  ↓
  更新 last_real_call_at

[主动路径] 后台调度器：
  扫描所有免费模型
  ↓
  检查 last_real_call_at
  ├─ < 4 小时前有真实调用 → 跳过，被动信号已足够
  └─ ≥ 4 小时无调用 → 触发主动探测（频率：每 2 小时一次）
       ↓
       构造最小请求（如 "hi"，max_tokens=1）
       带 10s 超时调用
       ↓
       分类结果：
         ├─ 成功 + 响应 < 阈值      → 🟢 Healthy
         ├─ 成功 + 响应 ≥ 阈值      → 🟡 Slow
         ├─ 429 限流               → 🔴 Down (rate_limited)
         ├─ 401/403 鉴权失败       → 🔴 Down (auth_failed)
         ├─ 404 模型不存在         → 标记下线
         ├─ 5xx / 网络错误         → 🔴 Down (server_error)
         └─ 超时                  → 🔴 Down (timeout)
       ↓
       配额保护检查：当日主动探测次数 > 该模型日限额 5% → 暂停探测
  ↓
  记录到健康历史表（保留 7 天）
```

### 5.4 用户使用模型的两种方式

**方式 A：直接调用（MVP 默认）**
```
用户在 Dashboard 看到模型可用
  ↓
点击「复制调用示例」
  ↓
粘贴到自己的代码中（用自己的 Key 直连厂商）
  ↓
调用厂商 API
```

**方式 B：通过统一接口（V1.0+）**
```
用户配置代码中的 base_url 为 Available Computing
  ↓
调用 /v1/chat/completions, model="available/llama-3.3-70b"
（stream: true 时走 SSE，必须支持）
  ↓
Available Computing 后端：
  ├─ 查询当前可用 Channel
  ├─ 选一个（按策略：随机/轮询/最快响应）
  ├─ 转发请求到厂商，透传 SSE 流（不缓冲，逐 chunk 返回）
  ├─ 故障转移：厂商报错 → 自动换下一个 Channel 重试
  └─ 记录调用日志（含 token 数、耗时、是否回退）
  ↓
返回响应（流式 / 非流式）给用户，接口行为与 OpenAI 完全一致
```

---

## 6. 免费模型判定策略（核心）

这是产品的最核心算法，单独详述。

### 6.1 判定流程（按顺序）

```
对一个 Model 判定 is_free:
  ↓
[Step 1] 厂商级标记
  ├─ 内置数据：Groq、Cloudflare 等整厂商免费 → is_free = true, source = "provider_free"
  └─ 否则继续
  ↓
[Step 2] API 字段
  ├─ 模型对象中有明确字段如 pricing.input == 0、tags 含 "free"
  │   → is_free = true, source = "api_field"
  └─ 否则继续
  ↓
[Step 3] 白名单匹配
  ├─ 模型 ID 命中维护的白名单（如 SiliconFlow 的特定模型）
  │   → is_free = true, source = "whitelist"
  └─ 否则继续
  ↓
[Step 4] 兜底
  ├─ 三个信号都无法判定 → is_free = unknown, source = "unknown"
  ├─ UI 以不同样式展示（灰色标签）并提示用户："免费状态未知，请查阅厂商文档确认后再使用"
  └─ 系统不发探测请求来"猜"是否免费——厂商不在响应体中告知计费，只在账单中体现
```

### 6.2 透明度要求

UI 上每个被标记为「免费」的模型，必须同时展示：
1. **免费类型标签**（永久免费 / 免费配额 / 新用户赠送 / 未知），防止用户无感超额
2. **判定来源**（悬停 / 点击查看）

```
🟢 永久免费 ⓘ
   ├─ 来源: 白名单
   ├─ 白名单条目: siliconflow/Qwen2.5-7B (v2026.05.01)
   └─ 上次验证: 2 小时前

🟡 免费配额 ⓘ   ← 醒目样式，提示有额度上限
   ├─ 来源: 白名单
   ├─ 配额: 1500 RPD（超出后按量计费）
   └─ 剩余估算: 今日已用 38%（如可获取）
```

### 6.3 白名单数据结构（概念，非代码）

存储为 YAML 文件，按厂商组织：

```yaml
version: 2026.05.01
providers:
  groq:
    free_strategy: all              # 整厂商免费
    notes: "Groq 全部模型当前免费，速率限制有差异"

  siliconflow:
    free_strategy: whitelist
    free_models:
      - id: Qwen/Qwen2.5-7B-Instruct
        free_type: permanent          # 永久免费，无额度上限
        notes: "永久免费"
      - id: deepseek-ai/DeepSeek-V2.5
        free_type: quota              # 有每日/月额度，超额收费
        rate_limit: { rpm: 100 }

  google:
    free_strategy: whitelist
    free_models:
      - id: gemini-2.0-flash
        free_type: quota              # 1500 RPD 免费，超出按量计费
        rate_limit: { rpm: 15, rpd: 1500 }
      - id: gemini-1.5-flash
        free_type: quota
        rate_limit: { rpm: 15 }
```

### 6.4 白名单维护策略

| 问题 | 方案 |
|------|------|
| 谁维护？ | 项目维护者 + 社区 PR |
| 怎么更新？ | GitHub 上的 YAML 文件，应用启动 / 用户点击「检查更新」时拉取 |
| 多久更新一次？ | 计划每月 1 次基线更新，重大变化即时更新 |
| 用户能否覆盖？ | 可以——本地白名单优先级高于远程 |

---

## 7. 数据模型（概念层）

> 仅展示实体与关键字段，不涉及具体表结构。

### 7.1 实体

```
┌────────────────────────────────────┐
│ Channel                            │
├────────────────────────────────────┤
│ id                                 │
│ provider_type   (groq/siliconflow) │
│ name            (用户备注)          │
│ api_key         (加密)              │
│ base_url        (可选)              │
│ enabled                            │
│ created_at                         │
│ last_probed_at                     │
└────────────────────────────────────┘
              │ 1
              │ N
┌────────────────────────────────────┐
│ Model                              │
├────────────────────────────────────┤
│ id                                 │
│ channel_id                         │
│ model_id        (厂商原始 ID)       │
│ display_name                       │
│ category        (text/vision/code) │
│ context_length                     │
│ rate_limit      (json)             │
│ is_free                            │
│ free_type       (permanent/quota/grant/unknown) │
│ free_source     (枚举)              │
│ health_status   (healthy/slow/down)│
│ last_response_ms                   │
│ last_checked_at                    │
└────────────────────────────────────┘
              │ 1
              │ N
┌────────────────────────────────────┐
│ HealthRecord     (滚动 7 天)         │
├────────────────────────────────────┤
│ model_id                           │
│ checked_at                         │
│ status                             │
│ response_ms                        │
│ error_code      (可选)              │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ Whitelist        (单条记录或文件)    │
├────────────────────────────────────┤
│ version                            │
│ updated_at                         │
│ data           (yaml/json)          │
└────────────────────────────────────┘
```

### 7.2 关键约束
- 同一厂商可存在多个 Channel（多 Key），但 (provider_type, name) 唯一
- Model 不直接存厂商，必须通过 channel_id 关联
- 同一 model_id 在不同 Channel 下视为不同 Model 实例（健康状态分别记录）

---

## 8. 适配器（Adapter）抽象

为支持快速接入新厂商，定义统一的 Adapter 接口（概念层）：

```
interface ProviderAdapter {
    // 元信息
    getProviderId(): string         // "groq"
    getDisplayName(): string        // "Groq"
    getDefaultBaseUrl(): string

    // 核心能力
    validateKey(key, baseUrl): Result<void, AuthError>
    listModels(key, baseUrl): Result<Model[], Error>
    isFreeModel(model, whitelist): { is_free, source }
    healthCheck(model, key): Result<HealthInfo, Error>
}
```

新增厂商 = 实现一个 Adapter + 在白名单 YAML 中加一节 + 注册到 Adapter Registry。

---

## 9. 部署形态

### 9.1 推荐部署方式

```
# 推荐：通过 Docker Secret 传入密码，避免明文出现在命令行历史中
echo "yourpassword" | docker secret create ac_admin_password -

docker run -d \
  -p 8080:8080 \
  -v ./data:/app/data \
  -e ADMIN_PASSWORD_FILE=/run/secrets/ac_admin_password \
  --secret ac_admin_password \
  available-computing:latest

# 或使用环境变量（仅开发环境）
docker run -d \
  -p 8080:8080 \
  -v ./data:/app/data \
  -e ADMIN_PASSWORD=yourpassword \
  available-computing:latest
```

数据全部在 `./data` 目录，包括：
- SQLite 数据库（Channel、Model、HealthRecord）
- 加密后的 Key
- 本地白名单覆盖

### 9.2 系统组件

```
┌─────────────────────────────────────────┐
│           Available Computing           │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Web UI (React 单页)             │   │
│  └────────────┬────────────────────┘   │
│               │ HTTP                    │
│  ┌────────────▼────────────────────┐   │
│  │  API Server                     │   │
│  │  ├─ REST API                    │   │
│  │  ├─ WebSocket (推送状态变化)     │   │
│  │  └─ V1.0+: OpenAI 兼容代理       │   │
│  └────────────┬────────────────────┘   │
│               │                         │
│  ┌────────────▼────────────────────┐   │
│  │  Core Services                  │   │
│  │  ├─ Adapter Registry            │   │
│  │  ├─ Discovery Engine (定时)      │   │
│  │  ├─ Health Prober (定时)         │   │
│  │  └─ Whitelist Manager           │   │
│  └────────────┬────────────────────┘   │
│               │                         │
│  ┌────────────▼────────────────────┐   │
│  │  Storage (SQLite)                │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
         │
         ▼ 出站调用
   各厂商 API
```

---

## 10. 设计中暂未决策的点

文档中保留这些开放点，待评审后定稿：

1. **MVP 是否要做 OpenAI 兼容代理？** 当前定位 V1.0+，但若用户呼声高可能前置
2. **健康探测是否对用户配额敏感？** 默认 30min/次，是否应该提供更激进/更保守的预设
3. **是否做模型「智能推荐」？** 例如根据用户最近调用习惯推荐相似免费模型——可能是 V2.0 的事
4. **多用户/团队版** 何时考虑？目前明确不做，但需评估是否影响早期数据模型设计
5. **白名单是否引入"过期时间"？** 比如某条目超过 30 天未更新自动转为弱信号
6. ~~**Dashboard 默认排序逻辑**~~ —— 已决策：默认按响应时间升序（最快模型优先）

---

## 11. 下一步

1. 评审本文档与 PRD —— 确认 MVP 范围与开放问题决策
2. 决定技术栈（Python / Go / Node）
3. 决定是 Fork New API 还是从零实现（建议从零）
4. 输出技术架构文档（`03-architecture.md`）
5. 输出 MVP 任务拆解（`04-mvp-tasks.md`）
6. 开始 MVP 开发
